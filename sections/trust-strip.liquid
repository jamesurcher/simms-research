{% comment %}
  Trust strip — 4-column section under hero.
  Each column: optional icon + a value in display weight + a mono uppercase label.
{% endcomment %}

<section
  class="trust-strip color-{{ section.settings.color_scheme }}"
  style="
    --trust-strip-padding-block-start: {{ section.settings['padding-block-start'] }}px;
    --trust-strip-padding-block-end: {{ section.settings['padding-block-end'] }}px;
  "
>
  <div class="trust-strip__inner">
    {%- if section.settings.eyebrow != blank -%}
      <p class="trust-strip__eyebrow">{{ section.settings.eyebrow }}</p>
    {%- endif -%}
    <div class="trust-strip__grid">
      {%- for i in (1..4) -%}
        {%- assign value_key = 'value_' | append: i -%}
        {%- assign label_key = 'label_' | append: i -%}
        {%- assign icon_key = 'icon_' | append: i -%}
        {%- assign value = section.settings[value_key] -%}
        {%- assign label = section.settings[label_key] -%}
        {%- assign icon = section.settings[icon_key] -%}
        {%- if value != blank -%}
          <div class="trust-strip__column">
            {%- if icon != blank and icon != 'none' -%}
              <span class="trust-strip__icon" aria-hidden="true">
                {%- assign icon_file = 'icon-' | append: icon | append: '.svg' -%}
                {{ icon_file | inline_asset_content }}
              </span>
            {%- endif -%}
            <div class="trust-strip__value">{{ value }}</div>
            {%- if label != blank -%}
              <div class="trust-strip__label">{{ label }}</div>
            {%- endif -%}
          </div>
        {%- endif -%}
      {%- endfor -%}
    </div>
  </div>
</section>

{% stylesheet %}
  .trust-strip {
    padding-block-start: var(--trust-strip-padding-block-start, 48px);
    padding-block-end: var(--trust-strip-padding-block-end, 48px);
    background: var(--color-background);
    color: var(--color-foreground);
  }

  .trust-strip__inner {
    max-width: min(var(--normal-page-width), 1040px);
    margin-inline: auto;
  }

  .trust-strip__eyebrow {
    font-family: var(--font-mono--family);
    font-size: 0.6875rem;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    opacity: 0.55;
    margin: 0 0 2.5rem 0;
  }

  .trust-strip__grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    justify-content: center;
    column-gap: 0;
    row-gap: 1.5rem;
    border-block-start: 1px solid currentColor;
    padding-block-start: 1.75rem;
  }

  .trust-strip__column {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    align-items: center;
    text-align: center;
    padding-inline: clamp(0.75rem, 2vw, 2rem);
    border-inline-end: 1px solid rgb(var(--color-foreground-rgb) / 0.12);
  }

  .trust-strip__column:last-child {
    border-inline-end: none;
  }

  .trust-strip__icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: clamp(1.625rem, 1.25vw, 2rem);
    height: clamp(1.625rem, 1.25vw, 2rem);
    color: var(--color-foreground);
    opacity: 0.85;
    margin-block-end: 0.25rem;
  }

  .trust-strip__icon svg {
    width: 100%;
    height: 100%;
  }

  .trust-strip__value {
    font-family: var(--font-heading--family);
    font-weight: 400;
    font-size: clamp(1.5rem, 1.6vw, 2.125rem);
    line-height: 1;
    letter-spacing: -0.01em;
  }

  .trust-strip__label {
    font-family: var(--font-mono--family);
    font-size: 0.6875rem;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    opacity: 0.55;
    line-height: 1.3;
  }

  @media (max-width: 749px) {
    .trust-strip {
      padding-block-start: calc(var(--trust-strip-padding-block-start, 48px) * 0.75);
      padding-block-end: calc(var(--trust-strip-padding-block-end, 48px) * 0.75);
    }

    .trust-strip__grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      row-gap: 2rem;
    }

    .trust-strip__column {
      padding-inline: 0.5rem;
      border-inline-end: 1px solid rgb(var(--color-foreground-rgb) / 0.12);
    }

    /* Kill right-border on the right-side cell of each row */
    .trust-strip__column:nth-child(2n) {
      border-inline-end: none;
    }

    .trust-strip__value {
      font-size: clamp(1.25rem, 5.6vw, 1.5rem);
    }

    .trust-strip__label {
      font-size: 0.5625rem;
    }
  }

  @media (min-width: 750px) and (max-width: 989px) {
    .trust-strip__grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .trust-strip__column:nth-child(2n) {
      border-inline-end: none;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Trust strip",
  "tag": "section",
  "settings": [
    { "type": "header", "content": "Content" },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow (optional, mono caps)"
    },
    { "type": "header", "content": "Column 1" },
    {
      "type": "select",
      "id": "icon_1",
      "label": "Icon",
      "options": [
        { "value": "none", "label": "None" },
        { "value": "badge-check", "label": "Badge check" },
        { "value": "award", "label": "Award" },
        { "value": "truck", "label": "Truck" },
        { "value": "file-text", "label": "Document" },
        { "value": "shield-check", "label": "Shield" },
        { "value": "gauge", "label": "Gauge" },
        { "value": "flask-conical", "label": "Flask" },
        { "value": "map-pin", "label": "Map pin" },
        { "value": "package", "label": "Package" },
        { "value": "qr-code", "label": "QR code" },
        { "value": "microscope", "label": "Microscope" },
        { "value": "clipboard-list", "label": "Clipboard" },
        { "value": "list-filter", "label": "Filter" }
      ],
      "default": "award"
    },
    { "type": "text", "id": "value_1", "label": "Value", "default": "99%+" },
    { "type": "text", "id": "label_1", "label": "Label", "default": "Purity Guaranteed" },
    { "type": "header", "content": "Column 2" },
    {
      "type": "select",
      "id": "icon_2",
      "label": "Icon",
      "options": [
        { "value": "none", "label": "None" },
        { "value": "badge-check", "label": "Badge check" },
        { "value": "award", "label": "Award" },
        { "value": "truck", "label": "Truck" },
        { "value": "file-text", "label": "Document" },
        { "value": "shield-check", "label": "Shield" },
        { "value": "gauge", "label": "Gauge" },
        { "value": "flask-conical", "label": "Flask" },
        { "value": "map-pin", "label": "Map pin" },
        { "value": "package", "label": "Package" },
        { "value": "qr-code", "label": "QR code" },
        { "value": "microscope", "label": "Microscope" },
        { "value": "clipboard-list", "label": "Clipboard" },
        { "value": "list-filter", "label": "Filter" }
      ],
      "default": "file-text"
    },
    { "type": "text", "id": "value_2", "label": "Value", "default": "COA Included" },
    { "type": "text", "id": "label_2", "label": "Label", "default": "Full Documentation" },
    { "type": "header", "content": "Column 3" },
    {
      "type": "select",
      "id": "icon_3",
      "label": "Icon",
      "options": [
        { "value": "none", "label": "None" },
        { "value": "badge-check", "label": "Badge check" },
        { "value": "award", "label": "Award" },
        { "value": "truck", "label": "Truck" },
        { "value": "file-text", "label": "Document" },
        { "value": "shield-check", "label": "Shield" },
        { "value": "gauge", "label": "Gauge" },
        { "value": "flask-conical", "label": "Flask" },
        { "value": "map-pin", "label": "Map pin" },
        { "value": "package", "label": "Package" },
        { "value": "qr-code", "label": "QR code" },
        { "value": "microscope", "label": "Microscope" },
        { "value": "clipboard-list", "label": "Clipboard" },
        { "value": "list-filter", "label": "Filter" }
      ],
      "default": "flask-conical"
    },
    { "type": "text", "id": "value_3", "label": "Value", "default": "Third Party" },
    { "type": "text", "id": "label_3", "label": "Label", "default": "Lab Tested" },
    { "type": "header", "content": "Column 4" },
    {
      "type": "select",
      "id": "icon_4",
      "label": "Icon",
      "options": [
        { "value": "none", "label": "None" },
        { "value": "badge-check", "label": "Badge check" },
        { "value": "award", "label": "Award" },
        { "value": "truck", "label": "Truck" },
        { "value": "file-text", "label": "Document" },
        { "value": "shield-check", "label": "Shield" },
        { "value": "gauge", "label": "Gauge" },
        { "value": "flask-conical", "label": "Flask" },
        { "value": "map-pin", "label": "Map pin" },
        { "value": "package", "label": "Package" },
        { "value": "qr-code", "label": "QR code" },
        { "value": "microscope", "label": "Microscope" },
        { "value": "clipboard-list", "label": "Clipboard" },
        { "value": "list-filter", "label": "Filter" }
      ],
      "default": "truck"
    },
    { "type": "text", "id": "value_4", "label": "Value", "default": "2-Day" },
    { "type": "text", "id": "label_4", "label": "Label", "default": "Fast Delivery" },
    { "type": "header", "content": "Layout" },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "Top padding",
      "min": 0,
      "max": 200,
      "step": 8,
      "unit": "px",
      "default": 48
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "Bottom padding",
      "min": 0,
      "max": 200,
      "step": 8,
      "unit": "px",
      "default": 48
    }
  ],
  "presets": [
    {
      "name": "Trust strip"
    }
  ]
}
{% endschema %}
